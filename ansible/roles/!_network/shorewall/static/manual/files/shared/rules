# For information on the settings in this file, type "man shorewall-rules"
######################################################################################################################################################################################################
#ACTION		SOURCE		DEST		PROTO	DEST	SOURCE	ORIGINAL	RATE		USER/	MARK	CONNLIMIT	TIME		HEADERS		SWITCH		HELPER
#							PORT	PORT(S)	DEST		LIMIT		GROUP

# Firewall
INCLUDE rules.fw
# WAN
INCLUDE rules.wan
# DNAT
INCLUDE rules.wan-dnat
# ISCSI
INCLUDE rules.iscsi
# IPMI
INCLUDE rules.ipmi
# ESX
INCLUDE rules.esx
# WEB
INCLUDE rules.web
# ELSE
INCLUDE rules.else
# Bank of Moscow for DSZN Project via ipsec
INCLUDE rules.baom
# SQL
INCLUDE rules.sql
# VPN
INCLUDE rules.vpn
# KEY
INCLUDE rules.key
# ALL
INCLUDE rules.all