### VPC CREATE / VALIDATE 

- name: AWS | VPC | Run set Layout Zone Part
  ec2_vpc_net:

- name: AWS | VPC | Run get Layout Zone Part
  ec2_vpc_net_info:

### SUBNET FOR VPC ZONE

- name: AWS | VPC | Run set Zone Subnet
  ec2_vpc_subnet:

- name: AWS | VPC | Run get Zone Subnet
  ec2_vpc_subnet_info:

### NAT GATEWAY

- name: AWS | VPC | Run set Gateway of Zone Subnet
  ec2_vpc_nat_gateway:

- name: AWS | VPC | Run get Gateway of Zone Subnet
  ec2_vpc_nat_gateway_info:

